var colors ["red", "purple", "black"];
// Create an array called `colors` that contains three colors.
console.log("black");
// Log the last item in the array to the console.
var i=0; i < colors.red; i++)
for(colors[i]== input) {
  document.write("My favorite color is red");
}

var i=0; i < colors.purple; i++)
for(colors[i] == input) {
  document.write("My favorite color is purple");
}

var i=0; i < colors.black; i++)
for(colors[i] == input) {
  document.write("My favorite color is black")
}

function instructor(name);{
this.name="name";
}

var instructor=("firstName","lastName","facial-hair");
console.log(this.name)


// Create a `for` loop that prints out "My favorite color is [insert color here]" for each item in `colors`.

// Create an `instructor` object that contains three key-value pairs.

// Add a `facial-hair` property to `instructor`.

// Log the value of the `facial-hair` property to the console.


function()
